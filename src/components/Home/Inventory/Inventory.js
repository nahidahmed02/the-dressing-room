import React from 'react';

// will show 6 items in the home page
const Inventory = () => {
    return (
        <div>
            <h2>inventory</h2>
        </div>
    );
};

export default Inventory;